Methods score identical outputs pointwise without human labels or opponent information.
Table~\ref{tab:main-alignment} separates strict preference agreement from Best-of-$N$ accuracy and normalized rank regret.
Selection uses independently human-ranked output groups, not rankings reconstructed from incomplete pair labels.

\begin{table}[!htbp]
\caption{\textbf{C1: final held-out performance.}
Pair: preference agreement (\%); Bo$N$: Best-of-$N$ accuracy (\%); Reg: normalized rank regret; coverage (\%) uses requested inputs.
Values equally weight four domains with $N=4$.}
\label{tab:main-alignment}
\centering\small
\begin{tabularx}{\linewidth}{@{}Ycccc@{}}
\toprule
Evaluator & Pair $\uparrow$ & Bo$N$ $\uparrow$ & Reg $\downarrow$ & Coverage $\uparrow$\\
\midrule
\input{figures/ideal_scenario/main}
\bottomrule
\end{tabularx}
\end{table}

\begin{figure}[!htbp]
\centering
\input{figures/results_landscape_pending}
\caption{\textbf{C1 breadth and evaluation cost.}
(a) Candidate task families and output types.
(b) Four-domain agreement and Seed-normalized model cost.
(c) Agreement across tasks, with EvalOpt in lake green and baselines in family shades.
The same 50--100\% scale applies throughout (c).
Four domains reuse Table~\ref{tab:main-alignment}; fourteen additional assignments do not enter its mean, and four slots remain empty.}
\label{fig:c1-landscape}
\end{figure}

Figure~\ref{fig:c1-landscape} separates domain coverage from the four-domain comparison.
Search cost is distinct from the cost of deploying a frozen evaluator.
Deployment accounting includes actual model calls, tokens, tools, latency, and failures under a common cap (Appendix~\ref{app:adaptation}).
