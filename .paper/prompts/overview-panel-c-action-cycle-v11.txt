Use case: precise-object-edit
Asset type: full-width academic method overview, review candidate.
Input image 1: edit target. Preserve the complete outer four-panel composition, panel a's task panorama, panel b's preference mining artwork, panel d's applications, their wording, and the soft dimensional illustration style. Replace ONLY the interior of panel c and its immediate input/output connectors. Keep c's title, size, and lake-green palette. Do not copy c's existing internal arrangement.

Primary request: make panel c an exceptionally clear, spacious ACTION CYCLE, not a repeated candidate trajectory. One horizontal line of four evenly spaced pictorial actions, with two nested return paths below and a separate candidate-selection branch above. Readability and correct arrow destinations are more important than decorative detail.

Panel c composition (positions relative to its usable interior beneath the title):
- Main action row at 53% height. Four compact, softly shaded icons with bold two-line labels: at 15% width, a folder/lightbulb labeled "Propose direction"; at 40%, a program page with pencil labeled "Revise program"; at 65%, a program with measuring gauge labeled "Evaluate candidate"; at 88%, a notebook and magnifier labeled "Diagnose". Connect these four actions left-to-right with three short, straight arrows. Keep clear gaps between icons, text, and arrows.
- A very pale lake-green rounded field begins AFTER Propose direction and surrounds ONLY Revise program, Evaluate candidate, Diagnose and the short return below them. Small field heading: "Active direction d". No nested boxes inside this field. No repeated rounds.
- Directly under Diagnose, small readable text: "Observed changes / Next question". This is a compact diagnostic annotation, not another node or extra card.
- SHORT GREEN RETURN: from the lower-left port of Diagnose, down to 77% height, horizontally LEFT, then UP into the BOTTOM of Revise program. One arrowhead, only at Revise program. Label the horizontal segment "Continue direction · use diagnostics". Its meaning is diagnostic continuation, NOT automatic candidate-code inheritance.
- LONG GRAY RETURN: from the lower-right port of Diagnose, farther down to 92% height, horizontally LEFT underneath the green field, then UP into the BOTTOM of Propose direction. One arrowhead, only at Propose direction. Label "End direction → propose another". Keep this line distinctly below the green return, never crossing it. These two returns are alternatives, not sequential actions. Do not add a decision diamond, exit card, or loop enclosing the whole panel.
- INDEPENDENT SELECTION ABOVE: from the TOP of Evaluate candidate, a thin navy arrow goes UP, then RIGHT into one softly shaded program icon labeled "Best program B" in the upper-right, at about 88% width / 17% height. The branch annotation reads "Better agreement + checks: update B" with "Otherwise: keep B" just below it. This is the only best-program icon. There is no arrow from Diagnose to B and no arrow from B into either return path.
- "Checking set" is a SMALL data label immediately above Evaluate candidate, with one miniature data-sheet glyph. It is NOT a large process box, acceptance panel, or separate stage in the main row. Keep it visibly separate from the selection-branch annotation.
- From Best program B, one navy output connector goes RIGHT into panel d. A small lock and the label "After search" on this connector indicate that the final best program is frozen before downstream use. No other node connects to panel d. Preserve panel d's existing application illustrations and labels.
- Reconnect b's existing Human feedback output to the LEFT side of Propose direction. Route it in the empty left margin of c if needed; do not join either return line. It must have a visible arrowhead at Propose direction.

Style: retain the input's soft-volume illustrated icons, subtle paper shadows, rounded pale backgrounds, clear dark typography, and generous whitespace. Lake green #62AAA5, dark green #477D79 for the within-direction path; muted gray for the direction-change return; navy for best-program selection and output. Preserve panel a/b/d colors. Keep the fixed-base-model icon small in the header only if it fits without crowding.

Constraints: exactly four main actions, one best-program icon, one small checking-set label, two distinct return routes. NO circular ring. NO duplicate candidates, C1/C2/C3, summary example, component inventory, additional footer, flowchart diamonds, dense prose, invented scores, success guarantees, or forced minimum number of revisions. NO arrow crossings, lines through text, tiny lettering, or extra panels. Do not change a/b/d layout or replace their pictorial artwork with flat blocks. Do not add a banner or title outside the original panel titles.
