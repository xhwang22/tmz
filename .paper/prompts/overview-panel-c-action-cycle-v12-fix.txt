Use case: precise-object-edit
Input image 1 is the edit target. Make only three small diagram corrections to this existing academic figure. Preserve ALL artwork, text, colors, node positions, panel sizes, and the current overall layout except the explicitly named corrections. Do not redesign anything.

1. ACTIVE-DIRECTION FIELD: the pale green inner rounded rectangle incorrectly contains Propose direction. Move ONLY its LEFT boundary to the empty gap between Propose direction and Revise program. Its left edge must be to the RIGHT of the Propose direction icon/label, and to the LEFT of Revise program. Keep its other boundaries unchanged. Move the "Active direction d" heading slightly right if needed so it belongs inside the revised field. Propose direction MUST be visibly OUTSIDE this inner green field. Revise program, Evaluate candidate, Diagnose, and the short green return MUST stay inside. Do not move any action icon, action label, or action arrow. Keep the long gray return below the field, pointing up to Propose direction.

2. DOWNSTREAM CONNECTOR: retain the navy connector from Best program B through the lock marked "After search" into panel d. REMOVE the entire OLD BLACK distributor line in the narrow gutter between c and d, including all three old black arrowheads entering d's individual application cards and its bottom horizontal stub. This old black line has no source and is erroneous. Do NOT reconnect or replace it. There should be only ONE c-to-d connector: the existing NAVY Best-program output with lock. Keep d's application illustrations, internal arrows, labels, card backgrounds, and outer panel unchanged.

3. CHECKING INPUT: keep the small "Checking set" clipboard above the main row. Add ONE short thin muted-blue input arrow from this clipboard to the upper-left corner of the Evaluate candidate icon. Route it through the available empty space, clearly distinct from the existing vertical acceptance arrow above Evaluate candidate. Do not cross that vertical acceptance arrow or the Active direction heading. The arrowhead must point toward Evaluate candidate. Do not add a box, text, or another clipboard.

Everything else is locked, especially the two nested returns: short GREEN from Diagnose into Revise program; longer GRAY from Diagnose into Propose direction. Preserve the separate acceptance arrow from Evaluate candidate to Best program B, and Human feedback into Propose direction. No additional nodes, arrows, decorative elements, text changes, new panels, or numerical results. Preserve the same softly shaded pictorial artwork and lake-green palette.
