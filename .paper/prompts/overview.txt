Use case: scientific-educational
Asset type: a full-width conceptual overview figure for an ICLR research paper, not a poster or slide.
Primary request: illustrate learning an executable evaluation program from overall human preferences. ERA is the name of the revision METHOD in the middle panel, not the project or the paper. Produce a polished, publication-quality scientific illustration with meaningful artifact miniatures, layered program components, evidence cards, and carefully routed arrows. Every element must explain the method.

Composition: wide landscape, white background, three clearly separated panels, labeled (a), (b), and (c), with the central panel approximately twice as wide as each side panel. Large readable sans-serif labels, disciplined spacing, crisp dark outlines, restrained navy, teal, and warm ochre accents. Colored panel rules and direct labels must make the diagram readable in grayscale. Use flat editorial scientific illustration with only very subtle depth in the artifact and document stacks. Leave breathing room around every label. Do not put a large overall title in the image.

Panel (a), exact heading: "(a) Human preferences". Show a compact collection of three clearly synthetic example pairs: two slide cards, two browser-page cards, and two structured document pages. The two artifacts in each pair have the same type and task; never compare unlike types. Use tiny geometric content blocks, not real text or plots. These are illustrative candidate applications, not observed cases. Label the group "Task-specific pairs". Beneath the collection show a compact comparison card labeled "Overall preference". Label the source group "Proposal Train". These materials feed the revision method in panel (b), not the deployed evaluator. Do not show dimensional human ratings or human-written rationales.

Panel (b), exact heading: "(b) ERA: program revision". Draw an elegant central evaluation-program assembly with six individually labeled components, arranged compactly and hierarchically rather than as six generic empty boxes. Exact component labels: "Representation", "Rubric", "Specialists", "Tools", "Routing", "Synthesis". Use a document-view icon, a criterion card, a small organized group of judge cards, a magnifying glass/tool icon, a precise branching connector, and a merged evidence card respectively. No robot faces. Inside or immediately below the assembly put the exact label "Fixed base model". Above the assembly, a revision node labeled "Bounded revision" receives the human-preference input from (a). The program supplies a small stack of sourced evidence cards labeled "Execution evidence" back to this revision node; the revision node updates the assembly. These arrows form one clear contained learning loop, with no crossing arrows. Below the assembly, show a visually separate sealed box with a small lock, labeled "Sealed development". Its return arrow to the revision node is labeled "Aggregate feedback"; do not draw raw examples or labels leaving this box. No numerical performance values. Keep the full central panel legible and do not pack tiny prose into components.

Panel (c), exact heading: "(c) Pointwise evaluation". A small program artifact crosses a dashed boundary from the central panel on a one-way arrow labeled "Freeze". Within (c), a new generic structured-artifact card labeled "One artifact" enters a compact executable judge labeled "Frozen evaluator". Its output is labeled "Score + feedback" and branches to two small clean symbols labeled "Selection" and "Refinement". Put the exact short boundary annotation "No preference labels" below this inference path. At the foot of the diagram put a single short sentence: "One evaluator per evaluation standard". There must be no arrow carrying human labels, the comparison opponent, or sealed examples into this runtime. Do not depict a learned generator, parameter updates, a universal cross-domain scorer, or performance improvement.

Text: use only the exact short labels supplied above. Preserve spelling and letter case, particularly "ERA", "Sealed development", and "Pointwise evaluation". All labels must remain legible when the figure is printed at 5.5 inches wide. Panel headings are the largest type, node labels are medium type; avoid small type entirely.
Constraints: this is a method schematic, NOT experimental evidence. The miniatures are generic illustrations, not actual screenshots or benchmark cases. No fabricated scores, percentages, axes, bar charts, learning curves, experiment logs, version histories, intermediate failed methods, data-set names, private identifiers, citations, branding, watermarks, decorative brains, robots, glowing networks, or additional text. The visual hierarchy must distinguish human supervision, the ERA revision method, the editable evaluator program, sealed measurement, and label-blind deployment.
