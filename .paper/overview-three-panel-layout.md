# Figure 2 — approved three-panel overview

2026-09-21. The author confirmed manuscript integration after finding that
the paper still displayed the old circular figure. Canonical exports now live
in figures/overview.{svg,pdf,png}; the composition below is unchanged.
The author approved the wide 1840 × 800 composition, palette and pictorial
style, but asked to foreground what makes continued exploration possible.
The current revision replaces three equally prominent example-edit cards
with a close view of one unaccepted candidate, its retained diagnostics and
a targeted next revision. This local revision was explicitly authorized.

## Stable composition

- Left: preference mining. Keep four equally fitted task pictograms, existing
  datasets/new outputs, the signal comparison, random sample and human feedback.
  Signal agreement does not establish correctness.
- Center: depth-first evaluator evolution. Preserve the wide pale-green
  direction region and the shallow candidate-acceptance band underneath.
  The retained diagnostic record is now the focal object, not repeated code
  cards. No arbitrary component taxonomy or prescribed three-step process.
- Right: output selection, critic-guided refinement and planned reward-guided
  training. No outcome or efficacy claims are added.

Outer panel coordinates remain (20,20,350,742), (390,20,1090,742) and
(1500,20,320,742). Lake green #62AAA5 / #477D79, warm mining surfaces and cool
application surfaces are unchanged. Existing v12 pictograms are reused in
native SVG; no image-generation batch or new output versions are needed.

## Central mechanism

The subtitle states: Reject the candidate, retain the direction and diagnostics.
One shared direction is illustrated by making layout errors count in the
final score. The candidate adds a layout-error detector. A separate checking
result says No agreement gain / Not accepted; this is a constructed example.

The central paper record distinguishes:

- Observed in execution: Layout errors detected. Final scores unchanged.
- Agent's next question: Do these findings reach the scoring step?

The question is an interpretation to investigate, not a verified cause. The
next complete program illustrates passing findings into scoring. Its execution
observations return to the same record, allowing further questions/revisions.
No success, acceptance or minimum depth is asserted for this next revision.
The front paper shows this trial; layered paper edges suggest retained history,
not additional hidden claims or a mandatory structured schema.

Green links carry observations and diagnostic information. They do not claim
automatic code inheritance. The conservative parent rule still chooses the
working program; rejected code may be left behind while its observations remain.
The note Keep diagnostics after rollback makes that distinction explicit.

Dashed navy links send each candidate to the common checking procedure below.
Improved checking agreement plus the Method guards updates the best program;
No gain keeps the best program. Only the final frozen best goes to downstream
use. The two high-level decisions remain acceptance and continuation; parent
choice is not introduced as a third equal-level mechanism.

A gray exit leaves the shared direction region, not the rejection status or
the last revision. After the agent explicitly ends a direction, another may
start from the current best program if resources remain. The implementation
does not certify a sufficient stopping reason, semantic consistency, minimum
depth or exhaustion of all useful tests. Resource limits may leave it unresolved.

## Evidence and editing boundaries

The example illustrates method definitions, not an observed search trajectory.
Proposal cases supply detailed traces; checking supplies aggregate measurements.
Agent interpretations are not human rationales or automatically verified causes.
The method's empirical superiority remains unverified (C28); this artwork does
not close that gap. The visible constructed-example disclosure is retained.

Canonical Figure 2 and its caption now use this composition; the main text's
evolution references point to panel b, and the AI-use statement identifies the
reused pictograms. No method definition or empirical claim changes. The separate
author-requested Figure 1 surface refinement uses this proof's visual style but
preserves its own original words, photographs and search-tree geometry.

## Files and verification

- Generator: scripts/render_overview_layout_review.py
- Publish: add --publish to regenerate canonical SVG/PDF/PNG and manifest
- Manuscript: figures/overview.tex includes figures/overview.pdf at 5.4 inches
- Current SVG/PNG: build/overview-layout-current/overview.{svg,png}
- Provenance, labels and crop coordinates: build/overview-layout-current/manifest.json

Rendering checks text width against canvas/panel/local bounds and checks every
pair of exact font ink boxes for overlap. There are 79 labels. Source image
hashes, SVG parsing and 1840 × 800 output dimensions are checked separately.
The same three preview files are overwritten; no numbered directory is added.

This is not print-ready artwork: labels are 3.4–7.2 points at 5.4 inches and
some source pictograms remain below the raster-line-art resolution target.
These limitations do not change the approved composition during integration.
No GitHub push is part of this iteration.

## Manuscript integration verification

- Rebuilt paper.pdf: 36 pages; the new Figure 2 is on page 5. The rendered
  full page was inspected at build/figure-review/overview-page.png.
- check_overview.py verifies the canonical hashes, source image, all 79 vector
  labels, 18 embedded pictograms, native PDF dimensions, embedded fonts, panel
  references and the actual presence of the new diagram in the compiled paper.
- Measured label range: 3.4–7.2 pt. Pictograms: 231–1084 PPI. The existing
  print-size limitations are recorded rather than hidden by upsampling.
- Memory, terminology, whitespace, simulation and both landscape checks pass.
  The structural check reports only the existing 17 unused bibliography entries
  and 10 main-text pages against the 9-page initial-submission limit.
- Figure 1, numerical records and landscape artwork are unchanged by this
  integration. No generated result, new image-model call or GitHub push.
